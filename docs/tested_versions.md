# 動作確認済みバージョン

v1.0 リリース時点（2026-05-12）で動作確認した CLI のバージョン。

## CLI

| ツール | 確認済みバージョン | 備考 |
|---|---|---|
| Codex CLI | 0.135.0 | 主たる対象。Safe Auto Mode の隔離ドリル/launcher は 0.135 の新 sandbox 構文(`codex sandbox --permissions-profile`)と profile 分離(`safe.config.toml`)前提 |
| Claude Code | **最新版追従（固定をやめた・2026-08-20）** | 導入スクリプト（`0_AIツールをまとめて入れる`）と更新ボタン（`9_AIツールを最新版に更新`）は `@latest` を入れる。以前は `2.1.201` を固定インストールし、`policy/safety-policy.json` の `testedClaudeCodeVersion` を SSOT にして `launch-claude-safe.{sh,ps1}` / `診断.ps1` が版差を警告していたが、Claude Code 純正サンドボックス（`settings.mac.json` の `sandbox` 節）を主防御に据える方針転換にともない固定を撤廃し、policy のキーも削除した。キーが無いとき版照合を静かにスキップする実装だったので、警告はコード変更なしで止まる。最後に実測した版は `2.1.236`（`configs/tested-tool-versions.json` の `claudeCodeLastVerified`）。ランチャーは `claude --help` を見て対応フラグだけを付ける実装なので、版が上がっても起動は壊れない（未対応フラグの機能が落ちるだけ） |
| Gemini CLI | **0.41.2（凍結版）** | BeforeAgent / BeforeTool / AfterModel / AfterAgent hook。**2026-06-18 で公式廃止**（後継: Antigravity CLI） |
| Antigravity CLI (`agy`) | **1.0.0 / 1.0.1** | v1.3.0 で `launch-agy-safe.{sh,ps1}` を追加し並立対応。`--sandbox` 強制起動 + `proceed-in-sandbox` permission mode で防御。設定ファイル経由の deny キー有効性は未確認（v1.3.1 で実機受講者環境にて再検証） |

> **Claude Code の版を更新するときは、`policy/safety-policy.json` の `testedClaudeCodeVersion`（SSOT）と、
> user-facing に直書きされた `@x.y.z` リテラルを必ず同時に更新する**こと。直書き箇所:
> `0_AIツールをまとめて入れる-Mac.command` / `0_AIツールをまとめて入れる-Windows.bat` / `docs/05_Claude_Codeを安全に使う.md` /
> `docs/09_各AIのインストール.md` / `スタート.html` / `launch-claude-safe.{sh,ps1}` の不在時案内。
> 片方だけ変えると導入は旧版のまま入り、`launch-claude-safe`/`診断.ps1` の版差警告が全受講者に毎回出るドリフトになる。

## ⚠ Gemini CLI → Antigravity CLI 移行ステータス（v1.3.0 時点）

| 項目 | 状態 |
|---|---|
| Gemini CLI 公式廃止期限 | **2026-06-18**（Pro / Ultra / 無料ティア。Enterprise / Workspace は対象外） |
| Antigravity CLI 実機検証 | **完了**（agy 1.0.0 / 1.0.1 を yamaguchi 開発機で確認） |
| 本パッケージの agy 対応 | **v1.3.0 で並立対応**（Gemini CLI launcher と agy launcher の両方を提供） |

### agy 1.0.0 実機検証で判明した破壊的変更

- **CLI フラグ消失**: `--approval-mode` / `--policy` が agy には**存在しない**。現存フラグは `--add-dir` / `--sandbox` / `--dangerously-skip-permissions` / `--print` / `--prompt-interactive` のみ
- **設定パス**: `~/.gemini/antigravity-cli/settings.json`（互換ディレクトリ）
- **トップレベルキー**: `colorScheme` / `enableTelemetry` / `trustedWorkspaces` を確認。**`permissions.deny` 相当の glob deny スキーマは未確認**
- **MCP**: `url` フィールドが `serverUrl` に rename（破壊的）
- **Plugin import**: `agy plugin import gemini` で旧拡張を変換可能

### 講座運用での当面の方針（v1.3.0）

1. **Gemini CLI 利用者は引き続き `launch-gemini-safe.{sh,ps1}` を使う**（廃止期限 2026-06-18 まで）
2. **agy 利用者は `launch-agy-safe.{sh,ps1}` を使う**（v1.3.0 で新規追加）
3. 受講者が agy / Gemini CLI のどちらを入れていても、本パッケージの launcher 経由なら最低限の防御が効く
4. agy 起動後は `/settings` UI を開いて `configs/agy/recommended-settings.json` の値に揃えるよう案内
5. 2026-06-18 以降は Gemini CLI launcher を段階的に deprecate（v1.3.x で削除予定）

## OS

| OS | 確認済み | 備考 |
|---|---|---|
| macOS Sonoma 14.x | ✅ | doctor 10/10 PASS |
| Windows 10 21H2+ | 講師PC検証予定 | PowerShell 5.1 |
| Windows 11 | 講師PC検証予定 | PowerShell 5.1 / 7.x 両対応 |

## アップデートとの付き合い方

上記より新しいバージョンの CLI が出た場合：

1. スタート「8_安全パッケージを最新版に更新」→「9_AIツールを最新版に更新」の順に実行
2. スタート「10_困ったとき診断」を再実行
3. fail があれば [99_known_issues.md](99_known_issues.md) と [20_卒業後ガイド.md](20_卒業後ガイド.md) の「困ったときの調べ方」へ（在校中は講師へ）

仕様が大きく変わった場合は、本パッケージの新バージョンを待ってください。

---

> **ここから下は開発者向けの照合表です。受講生・卒業生は読まなくてかまいません。**
> この表は `install` が改ざん検知に自動で読み取るため、**削除・移動をしないでください**（無いとインストールが安全のため中止します）。

## 配布物 SHA-256 ハッシュ一覧（H6: 配布物改ざん検知）

`install.sh` / `install.ps1` 起動時、以下のファイルの SHA-256 を本表と照合する。
mismatch が出た場合、配布 URL すり替えや手動改変・破損の可能性があるため、
**既定でインストールを中止する**（何もコピーする前に exit 非0＝破損配布を弾く。2026-07 B1）。
講師が意図的に policy 等をカスタマイズする運用では、本表のハッシュを更新するか、
環境変数 `AI_SAFE_ALLOW_HASH_MISMATCH=1` を設定したときのみ警告付きで続行できる。

講師は新リリース時にこの表を更新すること。値は `shasum -a 256 <file>`（macOS）
または `Get-FileHash -Algorithm SHA256 <file>`（Windows）で算出する。

> ⚠️ **節の順序を入れ替えないこと。** installer は同じファイルの行が複数あっても
> **最初に見つかった 1 行しか見ない**（first-match）。そのため**現行版の節を必ず最上位に
> 置く**必要がある。古い版の節が上に来ると installer が過去のハッシュを引き、
> 全ファイルが不一致になって誰も導入できなくなる。
> この並び順は `scripts/release-version-check.sh` が検査しているので、
> 崩すとリリース前チェックが FAIL する。

### v1.0.x（過去履歴）

> これらは v1.0.x 時点のハッシュです。現行バージョンとは一致しません。参照のみ。
> install スクリプトはこのセクションを読みません（引用ブロック化により grep 対象外）。
>
> | ファイル (v1.0.x) | SHA-256 |
> |---------|---------|
> | [v1.0.x] policy/safety-policy.json | 6d4b9b3a2f7f37572d63f0a3c877b02072b38d91056234d3655a48935c1d73d5 |
> | [v1.0.x] configs/codex/hooks.mac.json | 6f03deee71871c40dd81d098867a4860284700f98135fbb05730936738a729ca |
> | [v1.0.x] configs/codex/hooks.windows.json | 9e7292426dd844ebe4d6ffa20f92f2283e9c1f6e704412bb1321117d4eb62d6a |
> | [v1.0.x] configs/claude/settings.mac.json | 03c7d38c28529afc4866ed500254ca8178426ae16f065b5248d662d7a156231f |
> | [v1.0.x] configs/claude/settings.windows.json | 460045ffb8d55636e98c529647fba93ea3bcd970a36b05af9bf673ee3f479444 |
> | [v1.0.x] configs/gemini/settings.mac.json | b9f45bac5583930c6b44a07a2351c6bd21722503de82983e63cd5f38db2a6213 |
> | [v1.0.x] configs/gemini/settings.windows.json | f061d04699ce366887ae829d0f6fd78ac8d597c9bbaac80bb985332f32d1f012 |
> | [v1.0.x] configs/codex/config.mac.toml | 0261597fa7cc1b4ba057d40e0b8dd05a0da0234e308efa42fcfb952d79d1b295 |
> | [v1.0.x] configs/codex/config.windows.toml | 6deb434337bb80bc4d0ed97c4b430ff844599bca27b90f1fc90310cce0340e00 |
> | [v1.0.x] configs/gemini/policies/safety.toml | d63830fc7548c9987a1d84b7ec0212b6527f639a6af808ee29d00427ceb87f3c |
> | [v1.0.x] workspace-template/aiexclude.template | 9fee69aa1fa5dc7253ebb1419bc1f28b4ca24c8c794f5c6fcc011a1c4a2e444b |

### v1.17.3（現行HEAD、実機で見つかった不具合の修正と、実用の妨げになっていた縛りの緩和）

> v1.17.2 からの変更: **安全規則（policy の deny 床）は不変で、packageVersion のみ更新。** 実機（Windows / mac）で見つかった不具合と、実用を妨げていた過剰な制限をまとめて解消。(1) **更新後に Windows で OpenCode / d-claude が起動しなくなる不具合を修正** — PowerShell 5.1 はネイティブコマンドの stderr を `NativeCommandError` に変換するため、gateway の指紋不一致（正常系）が `$ErrorActionPreference='Stop'` 下で終了時エラーになっていた。`gateway-token.js` の `--probe` を「終了コードだけで表現し黙る」形に変え、PowerShell 側は `Invoke-NativeQuiet` で受ける形に統一（**同種の危険箇所を全 .ps1 から洗い出し 8 箇所を修正**。d-claude 側にも同じ不具合があった）。(2) **素の `claude` / `codex` の deny を撤回し ask に変更** — 使いたい受講生を締め出していたため。安全な起動口（`claude-safe` 等）は従来どおり通す。(3) **ホーム配下の「道具の置き場」への書き込みを許可** — `~/.claude/skills/`・`~/.claude/commands/`・`~/.codex/prompts/`・`~/.config/opencode/command/` 等。**設定ファイル本体（`settings.json` / `config.toml` / `opencode.json`）と `~/.ai-safety/` / `~/.deepseek-claude/` は引き続き書き込み禁止**（AI が自分への制約を書き換えられないようにするため）。`..` 経由の踏み台も 3 エンジンとも遮断。副次で `~/.gemini/settings.json` が書き込み保護から漏れていた既存の穴を塞いだ。(4) **d-claude のモデル切り替えを追加** — 既定は `deepseek-v4-flash`、`/model deepseek-v4-pro` で切り替え可（その場かぎり）。`/モデル` で使い方を案内。(5) **macOS 標準の bash 3.2 で空配列の展開が `unbound variable` で落ちる不具合を修正** — 「PC 全体に安全設定を入れる」ボタンが全 mac 利用者で失敗していた。横断調査で「秘密を伏せる」ボタンにも同じ不具合を発見（引数なし＝説明書が案内する使い方で必ず落ちる状態だった）。(6) **通信の制限を許可リスト方式から拒否リスト方式へ** — 27 件のホワイトリストは実用で確実に詰まるため。匿名アップロード・匿名 paste・webhook 受信箱・使い捨てトンネルなど 45 件を拒否し、他は通す。**ファイルシステムの壁は維持**。実測で確認（普通のサイトと配布元は 200、危険な宛先は失敗、作業フォルダ外書き込みは拒否）。(7) **金庫のボタンを 3 つ新設**（上級 16/17/18）— OS 標準の金庫（mac=キーチェーン / Windows=DPAPI）に任意の秘密をしまう・取り出す・消す。値は画面に出さずクリップボード経由で渡し 60 秒後に自動消去（消去判定は平文でなく指紋で比較）。**パッケージの固定枠と受講生の自由枠を接頭辞で分離**し、名前に `.` を許さないことで偽装を防止。(8) 無害な調査コマンド（`Get-AppxPackage` 等）の誤検知が疑われた件は、3 エンジンに実文字列を食わせて**deny 床では止まっていないことを確認**（policy は変更せず、再発検知のため横断テストに 6 ケース追加）。


> v1.17.1 からの変更: **安全規則（policy の deny 床）は不変で、packageVersion のみ更新。** **Windows で `install.ps1` が `icacls /inheritance:r /grant:r "$env:USERDOMAIN\$env:USERNAME:(OI)(CI)F" /T` で権限を締めていたため、名前が解決できない環境（Microsoft アカウント / AzureAD 参加 / USERDOMAIN が期待と違う PC）で継承 ACL だけが消えて誰も権限を持たない状態になり、利用者本人が `~/.ai-safety` を読み書きできなくなっていた**（実機で確認: `gemini.dpapi` が作れない／`deepseek.dpapi` が読めない／診断が「PC を替えた可能性」と誤診）。(1) `/inheritance:r` を廃止し、`scripts/windows/repair-permissions.ps1`（新規）に集約。**「すでに本人が読み書きできるならアクセス権に一切触らない」を最初の分岐に置き、install が権限を壊す経路そのものを無くした**。(2) 修復は **実機で成功が確認された `icacls "<path>" /reset /T /C /Q` を第一の手段**とし、第二の手段として `GetAccessControl([AccessControlSections]::Access)` + `SetAccessControl` を用意（`Get-Acl`/`Set-Acl` コマンドレットは SACL を扱うため標準ユーザーでは `SeSecurityPrivilege` 不足で失敗する。実機で確認済み）。`takeown` は既定で実行しない（実機でアクセス拒否が大量発生）。(3) 締めたあとは必ず読み書きを検証し、失敗したら巻き戻す。(4) `14_フォルダのアクセス権を直す`（新規ボタン）と、診断・doctor・docs・スタート.html の案内を実機成功版に統一（cmd で実行する旨とエクスプローラー手順を併記）。(5) 診断が `UnauthorizedAccessException` を「PC を替えた可能性」と誤診していた問題、および doctor が権限エラー時に「平文の残骸なし＝合格」と誤判定していた問題を修正。**実機で失敗した4手法（SID の `*` 前置漏れ / takeown / Get-Acl+Set-Acl / 名前ベースの grant）は理由つきでコード内コメントに記録**。
> v1.17.0（卒業後の実用に耐える最小限の安全網へ）からの変更: **安全規則（policy の deny 床）は不変で、packageVersion のみ更新。** (1) 長時間おまかせモードを **Claude / Codex / OpenCode / AntiGravity × mac / Windows** の全組み合わせに対応（壁が無い環境では起動時に一度だけ同意を取る。deny 床・記録・恒久設定を汚さない方式は維持）。(2) `5_セーフOpenCodeを起動` を新設し AI 4 種をボタン 2〜5 に整列（旧 5〜12 は 1 つ繰り下げ。install の旧名掃除リストに追加）。(3) GPT-Image-2 の画像生成 MCP（`codex-image-mcp.js` / `generate_image_gpt`）を追加（Codex CLI 経由・API キー不要・保存先は作業フォルダ内に封じ込め・権限 ask 固定）。(4) **Windows で hook の日本語が文字化けする不具合を修正**（`[Console]::OutputEncoding` 未設定。`Emit-AssistedDecision` にも同一問題。`.bat` が `chcp 932` した実コンソールへ出すスクリプトは対象外とし境界をテストで固定）。(5) **Windows で 1 件目の鍵（Gemini）だけ金庫へ移行できない不具合に対処**（移行前にウォームアップ 1 回で全件を同条件に揃え、読み書きを 15 秒 → 温め直して 90 秒の 2 段構えに。失敗の理由をログと doctor に残す。原因の確定には実機ログが必要なため、起こりにくくする対策と可視化を同時に実施）。(6) `診断.ps1` で Gemini の平文警告が「DeepSeek キーの状態」の節に出ていた不具合を修正。(7) 金庫に既に鍵があるのに平文が残る場合の後片付けを追加（中身が食い違うときは両方残して警告）。


> v1.16.0（卒業後最終形 — docs 全面整理）からの変更: **「卒業後の実用に耐える最小限の安全網へ」という方針転換にともなう機能追加が中心。決定的 deny 床（`dangerousCommandRegex` / `redirectProtectedPathRegex` / `secretRegex` / `outputSecretRegex`）は 1 本も減らしていない。** (1) **過剰な縛りの撤去**: 授業で教えた使い方（AI から別の AI を呼ぶ・画像生成など）が deny/ask に阻まれて実行できなかった原因を取り除き、実用最小限まで緩めた。(2) **Claude Code 純正サンドボックスの導入（mac のみ）**: `configs/claude/settings.mac.json` に `sandbox` 節を新設（`enabled` / `autoAllowBashIfSandboxed` / `network.allowedDomains` / `network.deniedDomains`）。OS カーネル（Seatbelt）が作業フォルダ外への書き込みと許可外ドメインへの通信を強制的に止め、そのぶん確認ダイアログが減る。**サンドボックスは読み取りを止めないため、秘密の読み取りを止めている hook は 1 本も外していない。** Windows は公式非対応のため hook のみ（非対称の理由は `configs/claude/settings.windows.json` の `_comment_sandbox` に明記）。(3) **秘密の金庫**: API キーの保存先を平文ファイルから OS の資格情報ストア（macOS キーチェーン / Windows DPAPI）へ移し、既存の平文キーは install 時に自動移行する（`scripts/common/secret-store.js` / `secret-migrate.js`）。(4) **マスキングの対（伏せる／元に戻す）を新設**（`scripts/common/clipboard-mask.js`、スタート 上級10 / 上級11）。検出パターンは `scripts/common/secret-patterns.js` を SSOT として送信検査と共有し、改ざん検知の対象へ追加。(5) **「PC 全体に最低限の安全設定を入れる」を 4 エンジンへ拡張**（Claude / Codex / agy・Gemini CLI / OpenCode。`apply-global-{guard,codex,agy,opencode,deny}.js`）。Codex はデスクトップアプリにも同じ設定が効く。(6) **「新しい作業フォルダを安全にする」を新設**（`scripts/{macos,windows}/protect-folder.{sh,ps1}`）。(7) **「長時間おまかせモード」を新設**（`launch-claude-longrun.{sh,ps1}`）。mac のみで、**サンドボックスの中でだけ**承認を省く。Windows は壁が使えないため**起動を拒否する**のが仕様。(8) **Claude Code のバージョン固定を撤去**し最新版追従へ（policy から `testedClaudeCodeVersion` を削除。照合コードはキーが無ければ静かにスキップする実装なのでコード変更なし）。(9) **「最大保護モード」と bouncer-gateway（ローカル LLM 必須）を機能ごと削除**。これによりパッケージ全体からローカル LLM 依存が消えた。(10) **docs の全面見直し**: 守り方を「壁・見張り・記録係」の 3 分類に整理し、ツールごと・OS ごとに何が効くかを明記。新設 4 本。(11) **policy の変更は `packageVersion` と `protectedPathRegex` への `.deepseek-claude` 追加のみ**（同 hash 行の備考を参照）。

> v1.15.3（OpenCode に Playwright MCP を追加）からの変更: **卒業後最終形としてパッケージ全体を整理した。安全規則（policy の deny 床・guard・判定ロジック）は不変。** (1) **docs の全面再構成**: 既存ドキュメントの整理・番号再編に加えて新設 3 本（`docs/11_AGI-Cockpitで使う.md` / `docs/20_卒業後ガイド.md` / `docs/91_やさしい仕組み解説.md`）を追加。(2) **スタートフォルダの番号再編**: ボタンの並びを用途順に振り直した。(3) **「9_AIツールを最新版に更新」ボタンを新設**（`update-ai-tools.{sh,ps1}`）。(4) **workspace への docs 同期**: install が docs をワークスペースへ同期し、卒業後も手元で参照できるようにした。(5) **d-claude は維持**しつつ卒業後の移行案内を追記。(6) **ccmux を廃止**。

> **Playwright（ブラウザ自動操作・UIテスト）を OpenCode / d-claude の MCP として追加**: Microsoft 公式の `@playwright/mcp` を起動するラッパー `scripts/common/playwright-mcp.js` を新設し、OpenCode および d-claude の MCP 構成へ統合した。Web サイトの閲覧・スクリーンショット・UI テスト・クリックやフォーム入力などのブラウザ操作を AI から実行できる。ブラウザ操作は外部アクセスやフォーム送信等の副作用を伴うため、**権限は `playwright_*: ask`**（実行前に必ず確認を挟む）に固定。`AI_SAFE_DCLAUDE_PLAYWRIGHT=0` で個別無効化できる。

> v1.15.1 からの変更: **OpenCode を「裏で1件だけ実行する」入口をランチャーに追加**（`--title` / `--session` / `--task`、Windows は `-Title` / `-Session` / `-Task`）。対話画面(TUI)を開かず `opencode run --auto` を exec する経路で、可視化ダッシュボードの「雇用する」「話しかける」から使う。**安全設定（送信検査 Gateway・deny 床・承認モニター・ハーネス配置・利用者プラグインの承認）は、対話で起動したときとまったく同じものが効いた状態で走る。** 前提として、プラグインの承認は対話起動で一度済ませておく必要がある（裏実行では確認に答えられないため、未承認だと読み込まれない）。変更ファイル: `scripts/{macos,windows}/opencode/launch-opencode-deepseek.{sh,ps1}`、`policy/safety-policy.json`（packageVersion のみ）、`README.md`。


> v1.15.0 からの変更: **あわせて、OpenCode を「裏で1件だけ実行する」入口をランチャーに追加**（`--title` / `--session` / `--task`、Windows は `-Title` / `-Session` / `-Task`）。対話画面を開かず `opencode run --auto` を exec する経路で、可視化ダッシュボードの「雇用する」「話しかける」から使う。安全設定はすべて対話起動と同一のものが効いた状態で走る。前提として、プラグインの承認は対話起動で一度済ませておく必要がある（裏実行では確認に答えられないため未承認だと読み込まれない）。変更ファイル: `scripts/{macos,windows}/opencode/launch-opencode-deepseek.{sh,ps1}`。 **利用者プラグインの置き場 `<ワークスペース>/.ai-safety/plugins/` を install が作るようにした**。v1.15.0 は読み込む仕組みだけを入れ、置き場は利用者が自分で作る前提だったが、`.ai-safety` は隠しフォルダで Finder / エクスプローラーからたどるのが難しく、しかも `.ai-safety` は AI の書き込み禁止パスなので AI に作らせることもできない（＝**実質たどり着けない**）。対策= install（両OS）が空の `plugins/` と置き方を書いた `README.txt` を用意し（既にあれば触らない）、スタートフォルダに **「（上級）9_プラグインの置き場を開く」**（`.command` / `.bat`。追加当時の番号は 12）を追加してワンクリックで開けるようにした。ボタンは置き場が無ければ作ってから開く（v1.15.0 で導入済みの人が更新せずに使う場合の保険）。**保護面の変更はゼロ**: 用意するのは空の入れ物だけで、`.ai-safety` への AI の書き込み禁止も、顔ぶれ（名前＋中身のハッシュ）が変わったときだけ Enter を求める歯止めもそのまま。変更ファイル: `scripts/macos/install.sh`、`scripts/windows/install.ps1`、`workspace-template/スタート/（上級）9_プラグインの置き場を開く.{command,bat}`（新規。当時の番号は12）、`policy/safety-policy.json`（packageVersion のみ）、`docs/10_OpenCode_DeepSeekを安全に使う.md`、`README.md`。

> v1.14.20 からの変更: **利用者が自分の意思で置いた OpenCode プラグインを読み込めるようにした**。これまで設定ディレクトリの `plugin/` は「ランチャーが明示的に渡す Bouncer 監視プラグイン 1 本」に限る設計で、配布物の `plugin/` も作業フォルダの `.opencode/` も読まない（後者は `OPENCODE_DISABLE_PROJECT_CONFIG=1` による）ため、**利用者が自作のプラグインを入れる口が 1 つも無かった**。可視化ダッシュボード用のブリッジのような無害な用途まで塞がるのは不便なので、置き場を `<作業フォルダ>/.ai-safety/plugins/` の直下 1 段（`.js` / `.ts`）に限って開ける。設定ディレクトリの plugin は無条件で実行される＝そこに置いたコードは承認モニターの決定的 deny 床を通らないため、歯止めは「毎回、何を読み込むかを本人に見せること」に置いた（ファイルがあるときだけ名前を一覧表示し、心当たりが無ければ Ctrl+C で止められる旨を出す。無いときは何も出さない）。置き場そのものがショートカットのとき、および個々のファイルがショートカットのときは配置せず警告する。**維持したもの**: 配布物 `plugin/` を写さない判断 ／ `OPENCODE_DISABLE_PROJECT_CONFIG=1` ／ Bouncer 監視プラグインの明示渡し ／ deny 床。配置は既存の 2 つの fail-closed 検査（シンボリックリンク禁止・スラッシュコマンドの `` !` `` 禁止）より後段に置いた。この 2 つは「配布物のハーネスが差し替えられていないか」を見るためのもので、とくに `` !` `` の検査は .md のテンプレート展開を止める目的のため、感嘆符で終わるテンプレート文字列を含むごく普通の JavaScript が理由の分からない fail-closed で弾かれるのを避ける。変更ファイル: `scripts/{macos,windows}/opencode/launch-opencode-deepseek.{sh,ps1}`、`policy/safety-policy.json`（packageVersion のみ）、`docs/10_OpenCode_DeepSeekを安全に使う.md`、`README.md`。

> v1.14.19 からの変更: **★d-claude が「そのモデルは存在しない」で起動できない件を修正**。`ANTHROPIC_MODEL` 等に `deepseek-v4-flash[1m]`（1M コンテキスト指定）を設定していたが、**Claude Code 2.1.226 では `[1m]` を名前の一部として扱い**「There's an issue with the selected model (deepseek-v4-flash[1m])」で起動できない（動作確認済みは 2.1.201。実機で再現・確認）。DeepSeek が公開しているモデルは `deepseek-v4-flash` と `deepseek-v4-pro` の 2 つだけで、`[1m]` 付きは API 上も存在しない（`GET /models` で実測）。対策= モデル名から `[1m]` を外し、**1M コンテキストは `CLAUDE_CODE_MAX_CONTEXT_TOKENS=1048576` で伝える**（Claude Code 自身がエラー文でこの方法を案内している）。外しただけだと「認識しないモデルなので 200k として扱う」警告が出るが、この env で消える。対象= mac/Windows の統合ランチャーと d-claude 起動スクリプト（.command / .bat）、および各 README の表記。あわせて、install を実行するテストが実 HOME を汚さないよう HOME を隔離し（利用者の oc-safe に検証用パスが焼き込まれる事故を 2 度起こしたため）、忘れたら落ちる見張りテストも追加した。検証= node tests 293/293、mac install+doctor pass=47 fail=0、実機の gateway 経由で `claude -p` を実走し、`[1m]` 付きは同じエラーで失敗、`[1m]` なし＋`CLAUDE_CODE_MAX_CONTEXT_TOKENS` で警告なく正答することを確認。
> v1.14.4 からの変更: DeepSeek の既定モデルを `deepseek-v4-pro`（プレビュー版）から `deepseek-v4-flash`（正式版 DeepSeek-V4-Flash-0731、2026-07-31 リリース）へ差し替え。対象は DeepSeek 版 Claude Code 起動（mac `.command` / Windows `.bat` の ANTHROPIC_MODEL / OPUS / SONNET、`[1m]` 1M context 指定は維持）、Bouncer 統合ランチャー（`launch-integrated.{sh,ps1}`）、OpenCode 既定モデル（`opencode-config.js` の model / agent.bouncer.model）。HAIKU / サブエージェントは従来から `deepseek-v4-flash` のため不変。V4 Pro はモデル一覧に残し手動選択可。モデルIDは公式 Change Log（api-docs.deepseek.com）で正式版も `deepseek-v4-flash` のままであることを確認。`.bat` は CP932・`.ps1` は BOM+CRLF を実バイトで維持確認。
> v1.14.3 からの変更: Windows側のnpm/OpenCodeラッパーが、準備ログと設定JSONの間に改行を入れない形、および準備ログ自体をJSONで出す形にも対応。全波括弧からJSONオブジェクトを走査するが、OpenCodeの解決済み設定に固有のトップレベル構造を4キー以上持つものだけを候補とし、候補が複数なら拒否する。設定を特定できない場合はGatewayの合言葉を `REDACTED` に置換した診断ファイルをログフォルダへ残し、その場所を画面へ表示する。
> v1.14.2 からの変更: `opencode debug config` の標準出力に初回のBun依存関係準備ログが混ざると、設定JSON全体の解析に失敗してOpenCodeを誤停止していた。通常は出力全体を厳密にJSON解析し、失敗時に限って行頭から始まるJSONオブジェクトを一意に抽出する。複数JSONやJSON外の波括弧を含む曖昧な出力は拒否するため、安全な偽設定を前置・後置して弱い実設定を隠すことはできない。WindowsのUTF-8ファイル渡し（BOM・CRLF）とMacランチャー実走の両方で回帰確認。
> v1.14.1 からの変更: OpenCodeが管理する設定ディレクトリ直下の `node_modules` を、危険記法とシンボリックリンクの走査対象から除外。依存キャッシュ内の通常のJavaScriptテンプレートリテラル末尾 `!`＋バッククォートと `.bin` リンクで起動が止まる問題を修正した。`node_modules` 自体がリンクの場合、および依存キャッシュ外の未知ファイルにある危険記法・リンクは引き続きfail-closedで停止する。
> Mac / Windowsの「最新版に更新」は、配布ZIP内の `packageVersion` と更新後ワークスペースの `packageVersion` を照合し、一致した版を完了画面へ表示する。不一致は更新成功として扱わない。

> 実測日: 2026-07-26。OpenCode 1.18.4 + ローカル擬似OpenAI互換Providerで実測。
> `permission.asked` をBouncerへ表示し、非対話モードの `reject` を監査ログへ反映。
> 承認対象コマンドは実行されず、プラグインready・承認要求・回答の一連を確認。
> OpenCodeの複合読み取りコマンドを、検索対象・検索語・件数制限・変更有無・外部送信有無まで固定ルールで説明。右パネルから全コマンドと意味を再展開できることを1580×1100の実ブラウザ表示で確認。
> Geminiの `finishReason=MAX_TOKENS` は未完成回答として破棄し、出力上限を4096へ拡大。Nodeテスト118件が全件成功。
> d-claudeをMac / Windows統合ランチャーの正式な選択肢へ追加。`AI_SAFE_AGENT=d-claude` としてモニターを起動し、送信検査Gateway必須表示、Claude安全フック経由、Mac dry-runを確認。

> 実測日: 2026-07-28（配布検証まわり）。実行環境: macOS (Darwin 25.5.0) / PowerShell 7.6.2 / Node.js 同梱テストランナー。
> 配布ハッシュ検証を fail-closed 化。(1) 本表 `docs/tested_versions.md` 自体が無いときは検証を丸ごと飛ばさず install を中止する。(2) AI に読ませる指示書のうち `workspace-template/opencode-harness/` と `dist-opencode/` 配下は `.md` を再帰的に列挙し、ハッシュ行が無いファイルが 1 つでもあれば中止する。`dist-skills/` は再帰列挙ではなく、既知の `hearing-ladder/SKILL.md` 1 本だけを同じ fail-closed 検査にかける（＝`dist-skills/` に未登録のファイルを置いても install は止まらない。2026-07-28 レビュー 3 巡目 YELLOW-N1 の既知の限界として残している）。それ以外の一般ファイルは従来どおり警告のみで続行（受講者の導入は止めない）。講師向けの明示 override は `AI_SAFE_ALLOW_HASH_MISMATCH=1` と `AI_SAFE_ALLOW_UNLISTED_HARNESS=1`。
> Windows 版 `scripts/windows/install.ps1` は本表を `-Encoding UTF8` で読むよう修正。本表は BOM なし UTF-8 のため、未指定だと Windows PowerShell 5.1 が既定 ANSI（日本語環境では CP932）として復号し、日本語名のコマンドファイル 5 本が一覧照合もハッシュ照合も素通りしていた。pwsh で「同じ表を ANSI 読みすると日本語行が 0 行・UTF8 読みなら 1 行」「`-Encoding UTF8` だけを ANSI に差し替えた影武者関数は同じ改ざんを見逃す」ことを実測。
> `scripts/release-version-check.sh` に (a) 本表と `policy/safety-policy.json` のハッシュ照合 (b) 決定的 deny の規則本数の下限検査 を追加。`dangerousCommandRegex` を 20 本から 1 本に削ると RESULT: FAIL になることを実測（従来は PASS だった）。実バイト検査には `.bat` / `.cmd` の CP932 復号確認を追加し、OK=141 / FAIL=0。
> mac のローカル検査 Gateway 起動スクリプト（当時の「最大保護モード」用。v1.17.0 で機能ごと廃止・削除）を Windows 版と同じ判定（running / stopped / unknown）にそろえ、判定不能・空出力は日本語メッセージで中止するよう修正。偽の `lms` を使った実走 9 ケースで確認。
> 実バイト検査の CRLF 判定を厳密化。CR と LF の個数一致だけでは、行の途中に紛れた CR と CR の付かない LF が相殺して素通りするため（例: `a<CR><LF>b<CR>c<LF>`）、「LF で区切って CR で終わる行の数＝CRLF の個数」も一致することまで見るようにした。相殺ケースを実際に作って検出することを確認済み。
> 検査自体が空振りしていないかの点検も実施。`release-version-check.sh` の各検査を 1 つずつ壊して（.ps1 の BOM 除去 / .bat の LF 化 / 行頭 chcp 65001 / .bat の UTF-8 化 / 表のハッシュ書き換え / 表の削除）**6 件すべてが FAIL として検出され、復元すると PASS に戻る**ことを実測した。PowerShell 側のテストは「中止したこと」だけでなく「中止の理由」まで照合するようにし、期待文言をわざと外すとテストが落ちることも確認した。
> 2026-08-01: OpenCode統合版のGatewayに `/status` を追加し、Bouncer画面上部に常時表示のOpenCode runtime statusを追加。会話本文・生成本文は保存せず、モデル、cwd、thinking、文脈残量、トークン消費、出力速度だけを表示する。DeepSeek V4 Pro / Flash は1M context / 384K outputとしてOpenCode設定へ明示。デスクトップ 1440x900 とモバイル 390x900 のヘッドレスChromeスクリーンショットで表示崩れなしを確認。
> テスト件数: `node --test scripts/common/test/*.test.js scripts/common/test/*.test.mjs` = 278 件すべて成功（2026-08-03実測）。Mac配布ハッシュ実走テストはNFD正常導入とNFD改ざん中止を含む9件すべて成功。mac シェルテスト、PowerShell構文検査、WindowsファイルのBOM・CRLF検査も成功。`scripts/release-version-check.sh` は RESULT: PASS。

| ファイル | SHA-256 | 備考 |
|---------|---------|------|
| policy/safety-policy.json | f1776749f4ccd812703326633bf6e0f57543229550584593dd6671073c849685 | v1.17.3でpackageVersionを更新（安全規則は不変。旧hash=0463dba3…）。2026-08-21 通信を許可リスト方式から拒否リスト方式へ変更（依頼者裁定）: `blockedDomains` を 10 件から 45 件へ拡張。追加は匿名 paste 系（termbin.com / ix.io / paste.rs / dpaste.com / dpaste.org / paste.ee / controlc.com / privatebin.net / rentry.co / justpaste.it）、匿名アップロード系（catbox.moe / *.catbox.moe / bashupload.com / oshi.at / temp.sh / gofile.io / filebin.net / uguu.se / envs.sh / x0.at / tmpfiles.org / ufile.io / keep.sh / wormhole.app）、webhook 受信箱（webhook.site / requestbin.com / *.pipedream.net / *.beeceptor.com）、使い捨てトンネル（*.ngrok.io / *.ngrok-free.app / *.ngrok.app / *.trycloudflare.com / *.loca.lt / *.serveo.net / *.localtunnel.me）。既存 10 件は 1 件も外していない。`_comment_blockedDomains` に「ここが拒否リストの SSOT で、configs/claude/settings.mac.json の sandbox.network.deniedDomains と同一に保つこと」を明記。`allowedDomains`（WebFetch 用）は同日の追補で `["*"]` の 1 件へ変更（下記）。旧hash=59c6db46…。 2026-08-21 追補（依頼者裁定）: **WebFetch（調べ物）も許可リスト方式から拒否リスト方式へ変更**。`allowedDomains` を 15 件（developers.openai.com / platform.openai.com / docs.anthropic.com / code.claude.com / github.com / api.github.com / raw.githubusercontent.com / objects.githubusercontent.com / docs.github.com / gemini.google.com / ai.google.dev / cloud.google.com / registry.npmjs.org / pypi.org / files.pythonhosted.org）から `["*"]` の 1 件にし、原則すべての宛先を通す。塞ぐのは `blockedDomains`（45 件）だけで、**この 45 件は 1 件も外していない**。キーを消さず `["*"]` と明示するのは、`scripts/macos/lib/safety_policy.sh` の `_POLICY_REQUIRED_KEYS` と空チェック、`scripts/windows/lib/SafetyPolicy.ps1` の `Assert-PolicyFloor` がいずれも「必須キーが無い・空配列」を fail-closed で止める作りだから（消すと WebFetch が全部止まる）。これに合わせて照合関数へ `"*"` の特別扱いを追加（mac `_domain_matches_list` / Windows `Test-IsDomainMatch`。判定順は blocked が先で allowed より優先、という順序は不変）。`configs/claude/settings.mac.json` の `sandbox.network.allowedDomains` と同じ書き方に揃えた。実測（本ファイルそのままの policy を本物の guard-webfetch に食わせた結果）: mac / Windows とも pastebin.com・gist.github.com・0x0.st・gofile.io・catbox.moe・files.catbox.moe・webhook.site・*.ngrok-free.app・*.trycloudflare.com・*.workers.dev = BLOCK ／ example.com・docs.python.org・qiita.com・developer.mozilla.org・github.com = ALLOW ／ 127.0.0.1・192.168.1.1・file:// スキーム・入力に混ざった API キー = BLOCK（宛先以外の床は 1 本も外していない）。この ①②③ は `scripts/macos/test/policy-floor.test.sh` と `scripts/windows/test/policy-floor.test.ps1` の「WebFetch の宛先判定」節が回帰テストとして固定する。OpenCode は元から WebFetch を宛先リストで判定しておらず（`scripts/common/opencode-config.js` が `webfetch: ask`／長時間おまかせモードでは `deny` に固定）、本変更の影響を受けない。`dangerousCommandRegex` / `protectedPathRegex` / `redirectProtectedPathRegex` / `secretRegex` / `outputSecretRegex` は 1 文字も触っていない。旧hash=423a6778…。 2026-08-21 受講生クレーム対応（道具の置き場は開ける／設定そのものは閉じたまま）: (a) `toolboxWritablePathRegex`（4 本）を新設。受講者が自分の道具を増やすための置き場（`.claude/skills` `.claude/commands` `.codex/prompts` `.codex/skills` `.gemini/commands` `.gemini/skills` `.config/opencode/command` `.config/opencode/skills`）だけを書き込み保護から免除する。v1.17 で「PC 全体に最低限の安全設定を入れる」「新しい作業フォルダを安全にする」を入れ、『作業フォルダの中だけで使う』前提が外れたのに、グローバルのスキルを 1 枚も足せない状態だったため。免除は `..` を含むパスには効かない（置き場を踏み台に設定本体へ回り込ませない）。(b) `redirectProtectedPathRegex` の 3 本目に `gemini` を追加（`~/.gemini/settings.json` が書き込み保護から漏れていた）。**決定的 deny 床は 1 本も減らしていない。現物の規則本数は dangerousCommandRegex 21 / protectedPathRegex 17 / redirectProtectedPathRegex 11（内容は 1 本拡張）/ toolboxWritablePathRegex 4 / secretRegex 9**。3 エンジン（mac / Windows / OpenCode）の判定一致は tri-engine-parity.test.js の「道具の置き場は開ける／設定そのものは閉じたまま」グループ 13 ケースが、Write/Edit ツール経路は toolbox-writable.test.js が固定する。旧hash=1eab8b1d…。 v1.17.2でpackageVersionを更新（安全規則は不変。旧hash=359ff7ad…）。v1.17.1でpackageVersionを更新（安全規則は不変。旧hash=e034ab83…）。v1.17.0でpackageVersionを更新（安全規則は不変。旧hash=63a8a42b…）。2026-08-20 実用最小限への転換: (a) `protectedPathRegex` に `.deepseek-claude` を追加（DeepSeek の実キーが `~/.deepseek-claude/auth` に平文で置かれるのに保護対象外で、`cat ~/.deepseek-claude/auth` が 3 エンジンとも素通りしていた）。**現物の規則本数は dangerousCommandRegex 21 / protectedPathRegex 17 / redirectProtectedPathRegex 11 / secretRegex 9**。(b) `testedClaudeCodeVersion`（=2.1.201）キーを削除し、Claude Code を最新版追従に変更。**決定的 deny 床（dangerousCommandRegex / redirectProtectedPathRegex / secretRegex / outputSecretRegex）は 1 本も減らしていない。protectedPathRegex は 16→17 本に増えた。** 旧hash=6fc2e2d8…。 v1.16.0でpackageVersionを更新（安全規則は不変。旧hash=5e1d7490…）。v1.15.3でpackageVersionを更新（安全規則は不変。旧hash=33835b3d…）。v1.15.2でpackageVersionを更新（安全規則は不変。旧hash=af3835d4…）。v1.15.1でpackageVersionを更新（安全規則は不変。旧hash=ea2cffee…）。v1.15.0でpackageVersionを更新（安全規則は不変。旧hash=37097ee2…）。v1.14.20でpackageVersionを更新（安全規則は不変。旧hash=9fc8349b…）。v1.14.19でpackageVersionを更新（安全規則は不変。旧hash=f0f5187a…）。v1.14.18でpackageVersionを更新（安全規則は不変。旧hash=23af5411…）。v1.14.17でpackageVersionを更新（安全規則は不変。旧hash=aef672c0…）。v1.14.16でpackageVersionを更新（安全規則は不変。旧hash=70febb59…）。v1.14.15でpackageVersionを更新（安全規則は不変。旧hash=9cf37427…）。v1.14.14でpackageVersionを更新（安全規則は不変。旧hash=fe30ca80…）。v1.14.13でpackageVersionを更新（安全規則は不変。旧hash=231632a1…）。v1.14.12でpackageVersionを更新（安全規則は不変。旧hash=833c88d1…）。v1.14.11でpackageVersionを更新（安全規則は不変。旧hash=8e4dbbc6…）。v1.14.10でpackageVersionを更新（安全規則は不変。旧hash=448b1e0b…）。v1.14.9でpackageVersionを更新（安全規則は不変。旧hash=6c6e67df…）。v1.14.8でpackageVersionを更新（安全規則は不変。旧hash=8bead154…）。v1.14.7でpackageVersionを更新（安全規則は不変。旧hash=9e205459…）。v1.14.6でpackageVersionを更新（安全規則は不変。旧hash=814ec023…）。v1.14.5でpackageVersionを更新（安全規則は不変。旧hash=427e5c06…）。v1.14.4でpackageVersionを更新（安全規則は不変。旧hash=5ebe8541…）。v1.14.3でpackageVersionを更新（安全規則は不変。旧hash=18bd98c6…）。v1.14.2でpackageVersionを更新（安全規則は不変。旧hash=eae761dd…）。2026-07-28 (中核ガード最終): `protectedPathRegex` の境界を `(^|[^A-Za-z0-9._-])` へ拡張し、引用符が直前に来る形も捕捉。**現物（このハッシュの中身）の規則本数は dangerousCommandRegex 21 / protectedPathRegex 16 / redirectProtectedPathRegex 11 / secretRegex 9**。以下の履歴に出てくる `redirectProtectedPathRegex` 7 本は、レビュー 3 巡目で 11 本へ拡張する前の値。旧 hash=7f11c54a…。 2026-07-28 (レビュー3巡目・redirectProtectedPathRegex を 7→11 本に拡張。Windows の PowerShell プロファイルとスタートアップフォルダを保護対象に追加。旧 hash=9017ce71…) / (レビュー2巡目・カンマ区切り複合指定に対応。`chmod a+x,o+w` のように危険な指定が2番目以降にある形を捕捉。旧 hash=8cf8c321…) / (レビュー1巡目・中核ガード確定版): 決定的 deny 床の拡充にともなう最終版。`dangerousCommandRegex` は 21 本（`icacls` を追加）、`protectedPathRegex` 16 本、`redirectProtectedPathRegex` 7 本、`secretRegex` 9 本。旧 hash=05c8b768…。 同日の中間状態: `protectedPathRegex` に `.ai-safety` を追加、書き込み先専用の `redirectProtectedPathRegex` を新設、`chmod` の 777 指定を `-R` / `a+rwx` 形まで捕捉。旧 hash=fcd9fcd5…。このハッシュは install（両OS）と scripts/release-version-check.sh の両方が照合するため、ポリシーを変えたら必ずここも更新すること。 v1.14.1でpackageVersionを更新。旧hash=81dd4925…。2026-07-07 (C): `testedClaudeCodeVersion` キー (=2.1.201) を追加。Claude Code 動作確認済み版の SSOT。install が本ハッシュと照合するため更新必須。旧 hash=2276db83…（内容は下記 v1.12.1 の deny floor と同一）。v1.12.1 deny floor 網羅性修正（レビュー RED-A/B ＋ Codex 追補対応）: `cat${IFS}.env`・`.envrc`・`-execdir rm`・PowerShell alias(`del -Recurse`/`ls -r\|rm`)・`rd/s`・`diskutil eraseDisk`/`Remove-Partition`・`--output`系2段DL も追加捕捉。旧 v1.12.1 中間 hash=a932cb93…。 再帰削除を長オプション前置(`rm --force -r`)・split flags・`find -delete`/`-exec rm`・PowerShell 省略形(`-rec`/`-re`)・`gci -r \| rm` まで捕捉／`.env` 読取を head/tail/grep/sed/awk/cp/strings 等に拡大＋引用符・`@`前置・pathlib・curl 流出も捕捉／`format` を drive/slash 必須化し `git format-patch` 過剰ブロック解消＋`Clear-Disk`/`Format-Volume` 追加／`dd of="/dev/"` 引用符・backtick `\`curl\``・2段DL実行・空白 fork bomb・`pnpm/yarn/npm --workspace publish` を追加／`del /q` 単独の過剰ブロック撤廃。mac grep・.NET -match 両エンジンで 83 ケース(BLOCK 57/PASS 26)一致・本物 guard-bash 実機立証。packageVersion 1.12.1。旧 v1.12.0 hash=8acb93c5… |
| configs/codex/hooks.mac.json | 6f03deee71871c40dd81d098867a4860284700f98135fbb05730936738a729ca | v1.0.x から変更なし |
| configs/codex/hooks.windows.json | 4e55cf8fbffbe44f1023455c902934f00d4a81d2637ba54c495cfaded18ca97c | v1.7.2 で Codex 二重包み対策に -File 形式へ変更 |
| configs/claude/settings.mac.json | 0c2318b5bd629f821ecf358c285afe0dd0fb067c790d20447a7a66372654e3e0 | 2026-08-21 通信を許可リスト方式から拒否リスト方式へ変更（依頼者裁定）: `sandbox.network.allowedDomains` を 27 件から `["*"]` の 1 件にし、原則すべての宛先を通す。塞ぐのは `deniedDomains`（policy/safety-policy.json の blockedDomains と同一の 45 件）だけ。理由: 許可リスト 27 件では調べ物・ライブラリ取得・新サービス利用で詰まり、かつ mac の Claude だけが制限され他エンジン（Codex は network_access=true、OpenCode / Gemini は無制限）は素通しという不揃いだった。⚠️ `allowedDomains` は消してはいけない（実測確認）: Claude Code 2.1.236 では通信フィルタを起動するかの判定が `network.allowedDomains !== undefined` だけで決まるため、消すとフィルタごと止まり deniedDomains も効かなくなる（実測でも消すと全ドメインが FAIL になった）。`"*"` は照合関数が `pattern === "*"` を特別扱いして全ホストに一致させる。判定順は denied が先で allowed より優先（公式スキーマ: 『Domains that are always blocked, even if matched by allowedDomains』）。実測（Claude Code 2.1.236 / macOS / 本ファイルそのまま）: example.com=200・pypi.org=200・registry.npmjs.org=200（リスト外も通る）／pastebin.com・gofile.io・webhook.site=通信失敗（拒否リストで塞がる）／作業フォルダ外への書き込み=Operation not permitted（**ファイルシステムの壁は維持**）。`strictAllowlist` は引き続き入れない（拒否リスト方式と噛み合わず、プロジェクト設定からは効かない仕様）。permissions の allow / ask / deny と hooks は 1 行も変更していない。旧hash=8d4a0bde…。 2026-08-21 受講生クレーム対応: permissions.allow に道具の置き場 8 本（`Edit(~/.claude/skills/**)` 等）を追加し、permissions.deny に設定そのもの 7 本（`Edit(~/.claude/settings.json)` `Edit(~/.claude.json)` `Edit(~/.codex/config.toml)` `Edit(~/.gemini/settings.json)` `Edit(~/.config/opencode/opencode.json(c)`）を追加。`Write(path)` ではなく `Edit(path)` で書いているのは、Claude Code がファイル書き込みの権限照合に Edit/Read しか使わないため。既存の deny は 1 本も外していない。旧hash=164e1356…。 2026-08-20: (a) Claude Code 純正サンドボックス（`sandbox` 節）を新設。`enabled: true` / `autoAllowBashIfSandboxed: true` / `network.allowedDomains`（教材とパッケージが実際に使う 27 ドメイン）/ `network.deniedDomains`（policy の blockedDomains を移設）。**目的は縛りを増やすことではなく減らすこと**で、サンドボックス内で完結する bash は確認カード無しで走る。⚠️ サンドボックスが止めるのは書き込みと通信だけで読み取りは止まらない（Seatbelt プロファイルに `(allow file-read*)` がある）ため、**guard 系 hook は 1 本も外していない**。`failIfUnavailable` / `strictAllowlist` / `allowUnsandboxedCommands` は入れていない（理由は同ファイルの `_comment_sandbox` に記載）。(b) `.deepseek-claude` 配下の Read / Write / Edit を deny に追加。旧hash=892644e1…。 2026-07-28: `.ai-safety` 配下の Read / Write / Edit を deny に追加（安全パッケージ自身の書き換えを Claude Code 経路でも止める）。旧 hash=accffeaa…。 v1.12.0 教室プロファイル: defaultMode acceptEdits + allow 大幅拡大（読取/ビルド/install/git 定型/curl/wget）+ ask（git push/reset/checkout/rebase/sudo）。hooks は不変。初回体験修正で allow に低ストレス系 sed/awk/tar/unzip を追加（deny/ask は不変）。※`make*` はレシピ内で任意コマンドを hook 不可視に実行しうるため allow から除外し ask 層に戻した（Codex 指摘）。旧 hash=ac3a7b72…/76cfddc8… |
| configs/claude/settings.windows.json | e795df3bb5b597aa1c3010ce14a833a53cffd9a83bfb31dd4261ea4b25b39754 | 2026-08-21 受講生クレーム対応: permissions.allow に道具の置き場 8 本（`Edit(~/.claude/skills/**)` 等）を追加し、permissions.deny に設定そのもの 7 本（`Edit(~/.claude/settings.json)` `Edit(~/.claude.json)` `Edit(~/.codex/config.toml)` `Edit(~/.gemini/settings.json)` `Edit(~/.config/opencode/opencode.json(c)`）を追加。`Write(path)` ではなく `Edit(path)` で書いているのは、Claude Code がファイル書き込みの権限照合に Edit/Read しか使わないため。既存の deny は 1 本も外していない。旧hash=92161695…。 2026-08-20: `.deepseek-claude` 配下の Read / Write / Edit を deny に追加。**`sandbox` 節は意図的に置いていない**（Claude Code 純正サンドボックスはネイティブ Windows 公式非対応。バイナリに実装はあるが feature gate off で「The Windows sandbox is not enabled on this build.」を返す）。非対称の理由は同ファイルの `_comment_sandbox` に明記。保護の中身（deny 床・保護パス・秘密検出）は 3 エンジン共通の policy/safety-policy.json で mac と同一。旧hash=916a62db…。 2026-07-28: 同上（`.ai-safety` 配下の Read / Write / Edit を deny に追加）。旧 hash=83ead913…。 v1.12.0 教室プロファイル: 同上（PowerShell 系 allow 込み）。hooks は不変。初回体験修正で allow に低ストレス系 sed/awk/tar/unzip を追加（deny/ask は不変）。※`make*` は同上の理由で除外。旧 hash=39bc6b63…/64d653fe… |
| configs/gemini/settings.mac.json | b9f45bac5583930c6b44a07a2351c6bd21722503de82983e63cd5f38db2a6213 | v1.0.x から変更なし |
| configs/gemini/settings.windows.json | f061d04699ce366887ae829d0f6fd78ac8d597c9bbaac80bb985332f32d1f012 | v1.0.x から変更なし |
| configs/codex/config.mac.toml | 19503171cbcf7a3e828a523a73a3824ef7d620d9f802de684ff1ca7404eff23d | v1.12.0 教室プロファイル: approval_policy on-request + approvals_reviewer auto_review + network_access true |
| configs/codex/safe.config.toml | c96b62c2f748edb87a2aeb87b95f9f2ccedb2733c2fa78c5a19d36c5f93b857a | v1.12.0 教室プロファイル: on-request + auto_review + network_access true（config.*.toml と同期） |
| configs/codex/config.windows.toml | a29d775b798716fa523703831e146eed883e56ba8f8306a4fe2c524c18bc170c | v1.12.0 教室プロファイル: approval_policy on-request + approvals_reviewer auto_review + network_access true |
| configs/gemini/policies/safety.toml | d63830fc7548c9987a1d84b7ec0212b6527f639a6af808ee29d00427ceb87f3c | v1.0.x から変更なし |
| workspace-template/aiexclude.template | 9fee69aa1fa5dc7253ebb1419bc1f28b4ca24c8c794f5c6fcc011a1c4a2e444b | v1.0.x から変更なし |
| workspace-template/dist-skills/hearing-ladder/SKILL.md | daa3f5261db91d2eac54952b82112727f96f2ecae7f0198ea96161bd1848332f | 2026-07-06 追加: やさしい階段型ヒアリング(壁打ち)スキル。install が $workspace/.claude/skills/ へ配置し d-claude/claude が読む。Claude に読ませる prompt=実質コード相当のため配布ハッシュ検証対象に含める。2026-07-28 改訂: 階段の運用ルール・選択肢の作り方・詰まったときの逃がし方・輪テンプレ4種を追補。2026-07-28 配置先追加: install が `$workspace/.ai-safety/dist-skills/` にも同じものを置き、OpenCode 統合ランチャーが起動ごとに `$XDG_CONFIG_HOME/opencode/skills/<名前>/` へ配置する（OPENCODE_DISABLE_PROJECT_CONFIG=1 下では `.opencode/skills` は読まれないため）。 |
| workspace-template/dist-claude-commands/モデル.md | 4a8fbc694f1fcf913314d079c6aacb21102e843310c1b165416d650445934a90 | 2026-08-21 追加: d-claude（Claude Code）用の日本語スラッシュコマンド `/モデル`。install が `$workspace/.claude/commands/` へ配置し、Claude Code が起動時に読む（OpenCode 用の 5 本＝`opencode-harness/commands/` とは読み手が別なので置き場も別）。既定は `deepseek-v4-flash` のままで、`/model deepseek-v4-pro` がその場かぎりの切り替え（実測: Claude Code 2.1.236 は "Set model to … for this session only" と返し、設定ファイルを書き換えない）。ランチャー側は `ANTHROPIC_CUSTOM_MODEL_OPTION=deepseek-v4-pro` を渡して `/model` の一覧にも出す。モデルに読ませる指示書＝実質コード相当なので丸ごとハッシュ検証対象（登録漏れは install が中止）。**コマンド本文に `` !`コマンド` `` を書いてはいけない**（テンプレート展開時に受講者のシェルへ直接渡って実行される）。 |
| workspace-template/opencode-harness/AGENTS.md | 52f4a0fe594a84f85a4fc6035a78e1d792645f19ff9d7557b9eb2ea5b27de6f3 | **v1.17.3（旧hash=e003e18b…）**: 素の `codex` / `claude` の起動を「決定的に止まる」扱いから外した。§4-1 の行を削除し、§4-2（確認カードが出れば進める操作）に「素の `codex` / `claude`（`-safe` なし）＝禁止ではない。はいと答えれば起動する」行を追加。§6 の「必ず `-safe` で呼ぶ／裸は決定的に止まる」を「できるだけ `-safe`。素も禁止ではない。相手が素で使いたいと言ったら止めない」に書き換えた。`scripts/common/opencode-config.js` の `enforcedBashDeny()` から `codex*` / `claude*` を外し ask へ移した変更に追従するもの。 2026-08-20 実用最小限への改訂: §4 を「押しても進まない操作(4-1)」と「確認カードが出れば進める操作(4-2)」に分割し、`curl` / `wget` / `chmod -R` / `git push` / `npm publish` を 4-2 へ移した（同日の `enforcedBashDeny()` 見直しに追従）。§5 の「迂回しない」を「危ない手段での言い換えはしない／正規の別ルートを使うのは迂回ではない」に書き分け、「1 度止まったら目的そのものを諦める」を明示的に禁止。§6 に **「別の AI に手伝ってもらう」節を新設**し、`codex-safe` / `claude-safe` が正規の手順であること・送信先・裸の `codex`/`claude` は止まることを表で明記。§6 冒頭に「ここに挙げるのは代表例であって、これ以外を使ってはいけないという意味ではない」を追記。旧hash=08c562b4…。 2026-07-28 追加: OpenCode 用の日本語ハーネス本体。install が `workspace-template/opencode-harness/` 配下の `*.md` を再帰的にハッシュ検証して `$workspace/.ai-safety/opencode-harness/` へ配置し、起動時にランチャーが `$XDG_CONFIG_HOME/opencode/` へ毎回コピーする（OPENCODE_DISABLE_PROJECT_CONFIG=1 下では作業フォルダ側の指示書が届かないため）。モデルに読ませる指示書=実質コード相当のため検証対象。**この 7 ファイルは文面を 1 文字でも直したらハッシュ更新必須**（不一致で install が中止する）。更新は `find workspace-template/opencode-harness -type f -name '*.md' \| sort \| while read -r f; do echo "\| $f \| $(shasum -a 256 "$f" \| awk '{print $1}') \|"; done` で再生成する。 **v1.17.1（旧hash=94cdb03e…）**: 外部へ出る道具の表に `codex-image_generate_image_gpt`（GPT-Image-2。送信先は OpenAI）を追記し、本数を 4 → 5 に直した。 |
| workspace-template/opencode-harness/agents/sensei.md | 96c555897e85164dfe595b6136caffa4a1e791ef80a3e61b8b48825aece54021 | **`agents/*.md` の frontmatter 3 原則（追加・改訂時は必ず守る）**: (1) bash/edit/write は `permission:` ではなく **`tools:` で false** にする（`permission:` で書くと解決済み設定が `bash: {"*":"deny"}` になり、起動前の権限自己検証がエージェントによる上書きとみなして起動拒否する）。(2) **`tools:` に `question: true` の明示が必須**（Markdown で定義したエージェントは question ツールが既定 false になり、明示しないと選択肢での聞き返しができず自由記述に落ちる。組み込みの build/plan と設定 JSON 内で定義したbouncer は既定 true）。(3) **`read: true` と `grep: true` は書かないこと** — エージェント個別 permission は共通ルールの後ろに連結され最後に一致した行が勝つため、`read: true` と書くと共通側の `.env` 禁止がこのエージェントだけ外れる（実測: せんせいで `.env` の中身がモデルに渡った）。2026-07-28 (3) の穴を塞ぐため `read: true` の 1 行を削除してハッシュ更新。旧 hash=1d3f3a54…。 2026-07-28 追加: 読み取り専用「せんせい」エージェント。frontmatter は `tools:` で bash/edit/write を false にすること（`permission:` で書くと解決済み設定が `bash: {"*":"deny"}` になり、起動前の権限自己検証がエージェントによる上書きとみなして起動拒否する）。また **`tools:` に `question: true` の明示が必須**（Markdown で定義したエージェントは question ツールが既定 false になり、明示しないと選択肢での聞き返しができず自由記述に落ちる。組み込みの build/plan と設定 JSON 内で定義した bouncer は既定 true）。今後 `agents/*.md` を追加するときも同じ明示が必要。 **v1.17.1（旧hash=d2e0b1f0…）**: 「せんせい」は説明専門で何も作らない役なので、新設した `codex-image_generate_image_gpt` も他の画像生成と同じく false に固定した。 |
| workspace-template/opencode-harness/commands/あんぜん.md | 8e4985be88cf1487f3066d8a4e784ef8849f4da3d5b03bd5d1299f2bb10539d2 | 2026-07-28 追加: 日本語スラッシュコマンド。1.18.4 は `{command,commands}/**/*.md` を読む（単数・複数どちらでも可）。**コマンド本文に `` !`コマンド` `` を書いてはいけない**: テンプレート展開時に受講者のシェルへ直接渡されて実行され、permission の確認も承認モニターの決定的 deny 床も通らない（バイナリ内 `/!\`([^\`]+)\`/g` → shell 実行を実測）。ランチャーが起動前に**配置後の実ファイル**を検査し、1 つでもあれば起動を中止する。 |
| workspace-template/opencode-harness/commands/しらべて.md | 0f79ac7049fb9f437824c93f07313e7467d884bf22f08b426304fe3ad6aa4e87 | 2026-07-28 追加: 同上 |
| workspace-template/opencode-harness/commands/せつめい.md | 7173cac59c65af404fc8b91500ab5c7c07aa628894007c931dfb02160f2c20aa | 2026-07-28 追加: 同上 |
| workspace-template/opencode-harness/commands/そうだん.md | 1b417f451d820c8e44bc1c64e7709d162c9a25d85b8ceb48748a023ab25aef38 | 2026-07-28 追加: 同上 |
| workspace-template/opencode-harness/commands/なおして.md | 66256c904c24dac63ca96672ca1aa0b74992011c7df74df46cd0f8d4e945aa12 | 2026-07-28 追加: 同上 |
| scripts/common/codex-image-mcp.js | 805d2e2716ed0dd6e4271da23828eec277782b60f1f6c661bc87e8283c40ec93 | v1.17.1 新規: GPT-Image-2 による画像生成 MCP（ツール名 `generate_image_gpt`）。`codex exec --enable image_generation --skip-git-repo-check -s read-only` を子プロセスで呼び、Codex が `~/.codex/generated_images/` に置いた画像を作業フォルダの `generated-images/` へ回収する。**API キー不要**（ChatGPT のサブスクリプション）。既存の `agy-image-mcp.js` と同じ作りで、呼ぶ相手が agy → codex に変わるだけ。子プロセス起動と保存先の決定を含むため改ざん検知の対象。**プロンプトはそのまま OpenAI へ送られ、DeepSeek 向けの送信検査 Gateway は通らない**（既存 MCP 5 本と同じ扱い）。 |
| scripts/common/secret-store.js | de3aca6797a7285edfda8f8441a9a7253946d7c3704cf4cd47723ef40f3941db | 2026-08-21 汎用の金庫ボタン新設（スタート 上級16/17/18）に伴い**自由枠**を追加: 受講者が任意の文字列を名前付きで出し入れできる `userSet` / `userGet` / `userRemove` / `userList` / `userExists` / `userCopy` / `isValidUserName` と、CLI 口 `--user-set`（値は stdin から読む）/ `--user-list`（名前だけ出力）/ `--user-remove` / `--user-copy`（値はクリップボードへ入れ、標準出力には出さない）を追加。**固定枠と自由枠は接頭辞で分離している**: 固定枠 = キーチェーン `ai-safety.<名前>` / DPAPI `<名前>.dpapi`（ITEMS の 5 件だけ。従来どおり表に無い名前は `unknown secret name` で弾く）、自由枠 = キーチェーン `ai-safety.user.<名前>` / DPAPI `user.<名前>.dpapi`。名前は英数字・かな・漢字等に制限し、`/` `.` `..` 空白・制御文字・`user.` を含む名前を拒否する（パス traversal と injection 防止）。一覧は自由枠だけを返す（対話ダイアログが出る `security dump-keychain` は使わず、索引 `~/.ai-safety/user-secrets.index` を 0600 で置く。**索引には名前だけを書き、値は書かない**）。既存 API（`get` / `set` / `remove` / `exists` / `resolve` / `resolvedExists` / `legacyPaths` / `ITEMS` / `--has`）の署名と挙動は不変で、固定枠は 1 件も壊していない。外部コマンドは既存の `runOpts()`（`spawnSync` の timeout ＋ SIGKILL）を共用し、クリップボード操作にもタイムアウトを付けている。旧hash=c09b8788…。 2026-08-20 配布前修正: 起動スクリプトが「鍵が登録されているか」だけを聞ける最小 CLI（`--has <name>`）を追加。判定は resolve()（環境変数 → 金庫 → 旧平文）そのもので、値は出力しない。旧hash=7b1acaeb…。 v1.17.0 新規: 秘密の金庫（OS のキーチェーン / DPAPI へ API キーを預ける）。**平文ファイルへのフォールバックを含む中核なので改ざん検知の対象に入れた。** **v1.17.1 で金庫呼び出しの耐障害性を追加（旧hash=5d965313…）**: 金庫の読み書きは子プロセス（`security` / PowerShell）なので、起動が遅い PC では制限時間に間に合わずに失敗しうる。旧実装は書き込みが `if (r.status !== 0) throw` の一発勝負で、しかも失敗の理由を捨てていた（Windows 実機で `gemini.dpapi` だけが作られず、原因を追えなかった）。set / get とも「15 秒 → 失敗ならウォームアップして 90 秒でもう一度」の 2 段にし、失敗時は終了コード・シグナル・所要時間・stderr 先頭を `takeLastFailure()` で返す（**鍵の値は一切含めない**）。読み取りは『未登録』では再試行しない（正常な結果なので）。金庫が使えるかを見るだけの probe は対話プロンプトを持たず固まらないため、最初から 30 秒。制限時間は `AI_SAFE_VAULT_TIMEOUT_MS` / `..._RETRY_MS` / `..._PROBE_TIMEOUT_MS` で上書きできる（失敗させる向きにしか働かず、保護は弱められない）。 |
| scripts/common/secret-migrate.js | 3a9f64591a63a0ba1667772b79651cfbe1b425309ecd331e01de9f81cbf3be74 | v1.17.0 新規: 既存の平文キーを金庫へ移す（受講生の操作ゼロ）。移行元の削除を伴うため改ざん検知の対象。 **v1.17.1 で修正（旧hash=2d857c5f…）**: 「金庫に既にある」を `continue` だけで終えていたため、一度でも読み戻し検証に失敗した鍵は平文が永久に残っていた（Windows 実機で `gemini-api-key.txt` だけが残る事象）。金庫の値と平文が一致すれば片付け、食い違えば両方残して知らせるようにした。あわせて読み戻しを 1 回だけ再試行し（金庫の呼び出しは子プロセスなので導入直後の 1 件目が制限時間に間に合わないことがある）、旧平文の読み取りで UTF-8 BOM を明示的に落とす。 さらに **v1.17.1（旧hash=70b84b80…）**: 移行の本番に入る前に子プロセスを 1 度から回しして全件を同じ条件に揃える（1 件目だけが不利にならないようにする）。金庫へ書けなかったときは `vault-write-failed` として理由ごとログに残し、`status()` の `writeFailures` で doctor から見えるようにした。 |
| scripts/common/secret-patterns.js | 5769ee7ca35dbc360016bfce767c440bd1e69d4ec9a5c793d93689bf1ca08f21 | v1.17.0 で改訂＋改ざん検知の対象に追加: 秘密の検出パターン（マスキングと送信検査が共有する SSOT）。ここが緩むとマスキングが素通りする。 |
| scripts/common/clipboard-mask.js | db7643f5201f8a07d2f9c7fe863b4c9e7f37f0e2a032e1bc7506de970acd6833 | v1.17.0 新規: コピーした文章から秘密を伏せる／元に戻す（スタート 上級10・上級11）。伏せ字化の本体なので改ざん検知の対象。 |
| scripts/common/apply-global-guard.js | ac262e1bca2fae6c5396cac9e9faed44efae6a66e57b1fc65c8913d425aaf657 | v1.17.0 で改ざん検知の対象に追加: Claude のグローバル設定（deny 床 union ＋ guard の絶対パス hooks）を書く本体。 |
| scripts/common/apply-global-codex.js | 50ebf7bd3f404dc3b8dd53c108fe830b875f0c581ffd416b7e51775995e27639 | v1.17.0 で改ざん検知の対象に追加: Codex のグローバル設定（`sandbox_mode="workspace-write"` / approval / env 除外 / hooks）を書く本体。Codex デスクトップアプリにも同じ config.toml が効く。 |
| scripts/common/apply-global-agy.js | fcd390571284803ebce86583a2cfb024d90ded51cba75dd0dda96e278ec888d8 | v1.17.0 新規: agy / Gemini CLI のグローバル設定に guard の絶対パス hooks を配線する本体。 |
| scripts/common/apply-global-opencode.js | eaad925b7c3cb339867ed7c9296db1f0690716b3474812706d2d84c9ad75f49e | **v1.17.3（旧hash=2ee276e3…）**: 先頭コメントの SSOT 一覧を実装に合わせて更新（deny から `codex*` / `claude*` を外し ask 側へ）。コードの動作は SSOT 参照のままで不変。 v1.17.0 新規: OpenCode のグローバル設定に permission.bash の最小 deny / ask を反映する本体（OpenCode には hook 層が無いため）。 |
| scripts/common/apply-global-deny.js | ced3be84adb05bea70dbeb1dfc6fc61627e9a88c8634dd9af4607d0e594efab4 | v1.17.0 で改ざん検知の対象に追加: グローバル deny 床の union 処理。 |
| scripts/macos/apply-global-guard.sh | 93d0773cd3eab3861ce9166edb32cc35709ece9c28d13b831ce9d081d2647195 | **bash 3.2 空配列 hotfix（旧hash=315aec5e…）**: 実機で「（上級）5_このPC全体に最低限の安全設定を入れる」が `line 121: STATE_ARGS[@]: unbound variable` で必ず落ちていた不具合の修正。macOS 標準の `/bin/bash` は 3.2.57 で、`set -u` 下で要素ゼロの配列を `"${arr[@]}"` と展開すると unbound variable になる（bash 4.4 以降では起きないため開発機で再現しない）。`AI_SAFE_GLOBAL_STATE` 未設定時に `STATE_ARGS=()` のままだった 4 箇所を、空でも安全な `${STATE_ARGS[@]+"${STATE_ARGS[@]}"}` 形へ置換。**動作は不変**（要素があるときの展開結果は同一）。回帰は `scripts/macos/test/bash32-empty-array.test.sh` が静的スキャン＋実行の両方で検査する。 **v1.17.3（旧hash=712dbe16…）**: 案内文の訂正のみ。OpenCode のグローバル設定について「危険コマンドの禁止（… / 他エージェントの裸起動）」と書いていたが、裸の `codex` / `claude` は deny から ask（一度だけ確認）へ戻したため、「確認を挟むコマンド（git push / npm publish / 他エージェントの起動 など）」へ移した。処理は 1 行も変えていない。 v1.17.0 で 4 エンジン対応に拡張＋改ざん検知の対象に追加: 「（上級）5_このPC全体に最低限の安全設定を入れる」の mac 実体。 |
| scripts/windows/apply-global-guard.ps1 | 8c1fed7d63c574a282da2b2e512311ae91fc93e5785c42b0bb7e0e2682f83b88 | **v1.17.3（旧hash=fae0da78…）**: 案内文の訂正のみ。OpenCode のグローバル設定について「危険コマンドの禁止（… / 他エージェントの裸起動）」と書いていたが、裸の `codex` / `claude` は deny から ask（一度だけ確認）へ戻したため、「確認を挟むコマンド（git push / npm publish / 他エージェントの起動 など）」へ移した。処理は 1 行も変えていない。 v1.17.0 で 4 エンジン対応に拡張＋改ざん検知の対象に追加: 同上の Windows 実体（BOM 付き UTF-8 / CRLF）。 |
| scripts/macos/uninstall-global-guard.sh | 185c0ff795c0907d7ba5168384439794b67e98bbf3cc0265ea08016b6e4eb53c | **bash 3.2 空配列 hotfix（旧hash=df8b763c…）**: apply 側と同じ修正。`AI_SAFE_GLOBAL_STATE` 未設定時に `STATE_ARGS=()` のままだった 4 箇所を `${STATE_ARGS[@]+"${STATE_ARGS[@]}"}` 形へ置換。**動作は不変**。 v1.17.0 で 4 エンジン対応に拡張＋改ざん検知の対象に追加: 「（上級）6_PC全体の安全設定を解除」の mac 実体。 |
| scripts/windows/uninstall-global-guard.ps1 | b58aedbe50a5c1066ad0e5dc1b648f4d34f09423ebd4a6d0a72e07a00701efdd | v1.17.0 で 4 エンジン対応に拡張＋改ざん検知の対象に追加: 同上の Windows 実体（BOM 付き UTF-8 / CRLF）。 |
| scripts/macos/protect-folder.sh | 201b1e19f37ccf558a33074cf2d7e3ee71e392ce51de57a01dc7d48369d44c49 | 2026-08-20 配布前修正: 拒否リストの穴を塞いだ。(a) 対象パスを `cd -P`/`pwd -P` で実体解決（`/tmp` は `/private/tmp` への symlink なので論理パスのままだと判定をすり抜けた）。(b) `/tmp`・`/private/tmp`・`/var/tmp` 配下を「一時フォルダ」として拒否。(c) `/Volumes/<ボリューム名>`（起動ディスクを含むマウント全体）を拒否（ボリューム内に作った普通のフォルダは許可）。(d) 標準入力が端末でないときは確認を飛ばさず**中止**（従来はパイプ経由だと確認なしで install が走った）。旧hash=0ff3910a…。 v1.17.0 新規: 「（上級）14_新しい作業フォルダを安全にする」の mac 実体。任意のフォルダへ安全ルール・ガード・ランチャーを入れるので改ざん検知の対象。 |
| scripts/windows/protect-folder.ps1 | 0104d5457824b44959e7b8406c6fdefbad8709d30d590cad978a8ad5e23adf90 | 2026-08-20 配布前修正: mac 側と同じ穴を塞いだ。`%TEMP%` / `%TMP%` / `%SystemDrive%\Temp` / `%WINDIR%\Temp` 配下を「一時フォルダ」として拒否し、対話できない実行方法では確認を飛ばさず中止する（ドライブ直下 `C:\` は従来どおり拒否）。旧hash=3f1d6f98…。 v1.17.0 新規: 同上の Windows 実体（BOM 付き UTF-8 / CRLF）。 |
| scripts/macos/launch-longrun.sh | 7d534e5966c5a11b94ef9a70abd2cb04117814001e22edb4f820c9bc20db0741 | **2026-08-21 (d-claude のモデル切り替え)**: d-claude が渡す `ANTHROPIC_CUSTOM_MODEL_OPTION` / `_NAME` / `_DESCRIPTION` も unset 対象に追加した（長時間おまかせ・claude-safe に d-claude 用の env を持ち越さない置き土産封鎖の延長。安全規則は不変）。旧hash=70cef33c…。 v1.17.1 で `launch-claude-longrun.sh` から改名・全面書き換え（旧hash=d8584e78…）。**「長時間おまかせモード」を全 AI・全 OS へ開いた**: Claude / Codex / OpenCode / agy を起動時のメニューから選べる。壁（OS サンドボックス）が効く組み合わせ（mac の Claude / mac の Codex）では従来どおり内容を表示して Enter、壁が無い組み合わせ（OpenCode / agy）では「この環境には壁がありません。止まるのは危険コマンドの禁止リストだけです。目を離す前提で続けますか」と表示して **「はい」の入力を 1 回だけ求める**（v1.17.0 までの「壁が無ければ起動を拒否」は撤廃。山口さん裁定）。Claude 経路の一時設定には従来どおり `sandbox.failIfUnavailable: true` を立てる（壁がある場合のみ。恒久設定には入れない）。deny 床・`disableBypassPermissionsMode: "disable"`・記録・「恒久設定を書き換えない（一時設定＋trap 削除）」は全経路で維持。`claude --help` などの外部呼び出しには perl alarm による上限を付けた。**承認を省く条件をこのファイルが担保するため、改ざん検知の対象。** |
| scripts/windows/launch-longrun.ps1 | 914f8318f08a408c443b40c3123ce2f58270315ab2d84b01c0a6b15a8fd93edd | **2026-08-21 (d-claude のモデル切り替え)**: d-claude が渡す `ANTHROPIC_CUSTOM_MODEL_OPTION` / `_NAME` / `_DESCRIPTION` も unset 対象に追加した（長時間おまかせ・claude-safe に d-claude 用の env を持ち越さない置き土産封鎖の延長。安全規則は不変）。旧hash=4a89b4b5…。 v1.17.1 で `launch-claude-longrun.ps1` から改名・**実質的に作り直し**（旧hash=1805c5ac…。旧版は理由を出して起動を拒否するだけだった）。mac 版と同じ 4 エンジンのメニューを持ち、Windows で壁があるのは Codex だけ（`windows.sandbox=unelevated`。書き込みは止まるがネット遮断は無い）なので、Claude / OpenCode / agy では「壁がありません」と表示して「はい」の入力を 1 回だけ求める。Claude 経路は sandbox 節を足さず（Windows には壁が無いため宣言だけして守れているように見せない）、ask を空にして deny 側へ寄せ、`disableBypassPermissionsMode: "disable"` を維持する。一時設定は `finally` で必ず削除する。BOM 付き UTF-8 / CRLF。**v1.17.3 で hash 更新**（旧hash=67809f70…）: `Invoke-Limited` の `Start-Job` 内で `$ErrorActionPreference = 'Continue'` を明示。PowerShell 5.1 はネイティブコマンドの標準エラーをリダイレクトすると `NativeCommandError` に変換するため、`Stop` のままだと「警告が 1 行出ただけ」で失敗扱いになる。 |
| scripts/windows/repair-permissions.ps1 | f809f945b6b68db4af94a25e98b2787595024fdc0edd2acc8bbf13956ac33349 | **新規（アクセス権の修復口）**: v1.17.1 までの install.ps1 は `icacls <~/.ai-safety> /inheritance:r /grant:r "$env:USERDOMAIN\$env:USERNAME:(OI)(CI)F" /T` で権限を締めていた。`/inheritance:r` が継承 ACL を全削除したうえで **文字列** `USERDOMAIN\USERNAME` に 権限を与える形で、この名前は Microsoft アカウント / AzureAD 参加 / USERDOMAIN が期待と違う PC では解決できない。解決に失敗すると「継承は消えたが誰も権限を持たない」フォルダが残り、**利用者本人ですら読み書きできなくなる**（実機で発生: gemini.dpapi を作れない・deepseek.dpapi を読めない・診断が「PC を替えた可能性」と誤診）。 **★ 実装は依頼者の Windows 実機での検証結果に合わせてある。** 成功したのは cmd で実行した `icacls "%USERPROFILE%\.ai-safety" /reset /T /C /Q` **だけ**で、実行後に暗号化された鍵が読め、鍵は無傷だった。よって本スクリプトの **第一の手段は `icacls "<path>" /reset /T /C /Q`**（親から継承される権限へ戻すだけなので、SID の書式も特権も所有権も関係しない。/T=配下も一括 /C=エラーでも続行 /Q=成功メッセージ抑制）。 **実機で失敗した方法（コード内コメントにも同じ記録を残してある）**: (1) `icacls ... /grant "<SID>:(OI)(CI)F"` → 「アカウント名とセキュリティ ID の間のマッピングは実行されませんでした」。icacls に SID を渡すには `*` の前置が必須。 (2) `takeown /F ... /R /D Y` → 「アクセスが拒否されました」が大量発生し、しかも不要だった。よって **takeown は既定で実行しない**（`-Takeown` を明示したときだけの最後の手段。install も修復ボタンも渡さない）。 (3) `Get-Acl` → `SetAccessRuleProtection` → `Set-Acl` → `SeSecurityPrivilege` が無く PrivilegeNotHeldException（この特権は所有者であっても既定では持っていない）。**`Get-Acl`/`Set-Acl` は使わない**。 (4) `GetAccessControl([AccessControlSections]::Access)` + `SetAccessControl` → **未検証**（(1)〜(3) の失敗後に `/reset` で解決したため実機で試す前に問題が消えた）。よってこれは **第二の手段**に置いてある。取得・書き戻しは .NET Framework(PS5.1) のインスタンスメソッドと .NET Core(PS7) の `FileSystemAclExtensions` 拡張メソッドの両方を反射で探す（PowerShell は拡張メソッドをインスタンス呼び出しに解決しないため。mac の pwsh 7 で実測確認）。`-SelfTest` で DACL 層の結線を実測できる。 そのほか: (a) **すでに本人が読み書きできる場合はアクセス権に一切触らない**ので、install（新規導入）が権限を壊す経路そのものが無い。(b) **継承の全削除をやめ**、広すぎる明示 ACE（Everyone / Authenticated Users / BUILTIN\Users / Guests / ANONYMOUS）だけを外す。(c) 変更のたびに **テストファイルの作成・書き込み・読み返し・削除と既存 *.dpapi の実読み込みで本人のアクセスを検証**し、失敗したら **変更前の DACL へ戻す**。(d) 管理者権限（UAC 昇格）は要求しない。(e) 外部コマンドはすべてタイムアウト付き。 install.ps1 は `-Quiet -NoTakeown` で本スクリプトを呼ぶ（install 自身は icacls を実行しない）。受講者は スタート\14_フォルダのアクセス権を直す.bat から同じコードを実行でき、案内文（診断 / doctor / .bat / docs / スタート.html）はすべて実機成功版の cmd コマンドとエクスプローラー手順に統一してある。同じ Get-Acl/Set-Acl の誤りが `install.ps1` の mac フック読み取り専用化と `lib/SafetyPolicy.ps1` の `Set-AuditLogAcl` にもあったので同時に修正した。回帰テスト: scripts/common/test/acl-permissions.test.js。**Windows 実機で確認済みなのは `icacls /reset` を手で叩いた回復のみ**。ボタン経由・第二の手段・PS5.1 経路は未検証（未検証項目は docs/99_known_issues.md に列挙）。 |

### v1.14.0（Bouncer統合版、Version SSOT 統一 + release-version-check.sh 新規）

> 実測日: 2026-05-28。`shasum -a 256 <file>` (macOS) で計測。
> v1.13.0 からの変更: Bouncer/OpenCode統合に伴い packageVersion を 1.14.0 に bump。
> 全 active docs / installer / workspace-template の version 表記を v1.14.0 に統一。
> `scripts/release-version-check.sh` 新規作成（SSOT drift 検出スクリプト）。
> 個別ファイルの SHA-256 一覧は、現行版である上の v1.14.4 節の表に集約した（v1.14.0 時点の値は git 履歴を参照）。

### v1.4.3（BOM 二重 hotfix）

> 実測日: 2026-05-28。`shasum -a 256 <file>` (macOS) で計測。
> v1.4.2 の追加致命バグ (doctor.ps1 / launch-claude-safe.ps1 に BOM が二重に入っており PS 5.1 起動不能) を緊急 hotfix。v1.4.2 の Release は未公開のままスキップし、v1.4.3 を正規リリースとする。

| ファイル | SHA-256 | 備考 |
|---------|---------|------|
| policy/safety-policy.json | 92a525342e5bb79e6138f47f8e5189c8bdc6a76bb7d2953d716bc9696abc737a | v1.4.3 で packageVersion を 1.4.3 に bump |
| configs/codex/hooks.mac.json | 6f03deee71871c40dd81d098867a4860284700f98135fbb05730936738a729ca | v1.0.x から変更なし |
| configs/codex/hooks.windows.json | 4e55cf8fbffbe44f1023455c902934f00d4a81d2637ba54c495cfaded18ca97c | v1.7.2 で Codex 二重包み対策に -File 形式へ変更 |
| configs/claude/settings.mac.json | 38b720bbe14be938574c7411913ad7c01313bfb4c8ebefd02d0f5d8d6b4740d6 | v1.4.1 で env exfil deny 追加 |
| configs/claude/settings.windows.json | 2886773553d02d868cb01501a1ce3683891bea2dc88e803654f4d8c24dc79661 | v1.4.1 で env exfil deny 追加 |
| configs/gemini/settings.mac.json | b9f45bac5583930c6b44a07a2351c6bd21722503de82983e63cd5f38db2a6213 | v1.0.x から変更なし |
| configs/gemini/settings.windows.json | f061d04699ce366887ae829d0f6fd78ac8d597c9bbaac80bb985332f32d1f012 | v1.0.x から変更なし |
| configs/codex/config.mac.toml | ba8c3ec6603ae2812918683a8f46c828a79cc432f22323ea9c218672aa029791 | v1.4.1 で `features.hooks=true` 修正 |
| configs/codex/config.windows.toml | 005fbee210b77482cb68912610b551a124608a192992346956a57cc724388537 | v1.4.1 で `features.hooks=true` 修正 |
| configs/gemini/policies/safety.toml | d63830fc7548c9987a1d84b7ec0212b6527f639a6af808ee29d00427ceb87f3c | v1.0.x から変更なし |
| workspace-template/aiexclude.template | 9fee69aa1fa5dc7253ebb1419bc1f28b4ca24c8c794f5c6fcc011a1c4a2e444b | v1.0.x から変更なし |

## v1.1.0 配布 zip 全体

| ファイル | SHA-256 |
|---------|---------|
| ai-agent-safety-package-v1.1.0.zip | d906e3f025f95d7cf9fc1e9066eb1bcdef088bd561fd07947fd002a88b0299bf |

このハッシュは配布元と受講者で一致することを確認してください:
`shasum -a 256 ai-agent-safety-package-v1.1.0.zip` (mac) / `Get-FileHash ai-agent-safety-package-v1.1.0.zip` (win)

## v1.2.0 配布 zip 全体

v1.2.0 では agent-monitor（承認時の解説カードと監視ビューア）を追加。
個別ファイル（policy/safety-policy.json 等）の SHA-256 は v1.1.0 から変更なし。
新規ファイル群（`configs/safety/cards/`, `scripts/{macos,windows}/monitor.{sh,ps1}`,
`scripts/{macos,windows}/lib/{explainer.sh,Explainer.ps1}`）はパッケージ zip 全体
のハッシュで検証する。

| ファイル | SHA-256 |
|---------|---------|
| ai-agent-safety-package-v1.2.0.zip | b7e5417d372c2ed4c60b12ed5c8603ea17b08bcdb68976ae24756eaaf3fb8281 |

このハッシュは配布元と受講者で一致することを確認してください:
`shasum -a 256 ai-agent-safety-package-v1.2.0.zip` (mac) / `Get-FileHash ai-agent-safety-package-v1.2.0.zip` (win)

## バックアップ整合性検証（H7: zip-slip / 改ざん）

`backup.sh` / `backup.ps1` は zip 作成時に同名の `.sha256` ファイルを出力する。
`restore.sh` / `restore.ps1` は復元前に以下を検証する。

1. **ハッシュ照合**: `<zip>.sha256` が存在すれば zip の SHA-256 と照合し、不一致なら exit 1
2. **zip-slip 検知**: エントリ名に `../` を含む場合、悪意あるアーカイブとして exit 1
3. 上記が通った場合のみ展開

`.sha256` ファイルが無い場合（旧バックアップ等）はハッシュ照合をスキップするが、
zip-slip 検知は常に実行する。
