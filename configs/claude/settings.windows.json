{
  "env": {
    "AI_SAFE_ENGINE": "claude",
    "CLAUDE_CODE_NO_FLICKER": "1"
  },
  "cleanupPeriodDays": 120,
  "permissions": {
    "defaultMode": "acceptEdits",
    "disableBypassPermissionsMode": "disable",
    "allow": [
      "Read(./**)",
      "Edit(./**)",
      "Bash(ls*)",
      "Bash(pwd*)",
      "Bash(cat *)",
      "Bash(type *)",
      "Bash(head *)",
      "Bash(tail *)",
      "Bash(where *)",
      "Bash(tree*)",
      "Bash(echo *)",
      "Bash(dir*)",
      "Bash(date*)",
      "Bash(mkdir *)",
      "Bash(touch *)",
      "Bash(cp *)",
      "Bash(mv *)",
      "Bash(Copy-Item *)",
      "Bash(Move-Item *)",
      "Bash(New-Item *)",
      "Bash(Get-ChildItem *)",
      "Bash(Get-Content *)",
      "Bash(env*)",
      "Bash(printenv*)",
      "Bash(sed *)",
      "Bash(awk *)",
      "Bash(tar *)",
      "Bash(unzip *)",
      "Bash(git status*)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(git show*)",
      "Bash(git branch*)",
      "Bash(git add*)",
      "Bash(git commit*)",
      "Bash(git stash*)",
      "Bash(git clone*)",
      "Bash(git fetch*)",
      "Bash(git pull*)",
      "Bash(npm install*)",
      "Bash(npm i *)",
      "Bash(npm run*)",
      "Bash(npm test*)",
      "Bash(npx *)",
      "Bash(pnpm *)",
      "Bash(yarn *)",
      "Bash(pip install*)",
      "Bash(pip3 install*)",
      "Bash(gem install*)",
      "Bash(node *)",
      "Bash(python *)",
      "Bash(python -m pytest*)",
      "Bash(pytest*)",
      "Bash(curl *)",
      "Bash(wget *)",
      "Bash(Invoke-WebRequest *)",
      "Bash(Invoke-RestMethod *)",
      "WebFetch(domain:developers.openai.com)",
      "WebFetch(domain:docs.anthropic.com)",
      "WebFetch(domain:code.claude.com)",
      "WebFetch(domain:github.com)",
      "WebFetch(domain:raw.githubusercontent.com)",
      "WebFetch(domain:gemini.google.com)",
      "WebFetch(domain:ai.google.dev)",
      "WebFetch(domain:cloud.google.com)"
    ],
    "ask": [
      "Bash(git push*)",
      "Bash(git reset*)",
      "Bash(git checkout*)",
      "Bash(git restore*)",
      "Bash(git rebase*)",
      "Bash(sudo *)"
    ],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(Remove-Item * -Recurse * -Force *)",
      "Bash(npm publish*)",
      "WebFetch(domain:gist.github.com)",
      "WebFetch(domain:gist.githubusercontent.com)",
      "WebFetch(domain:pastebin.com)",
      "WebFetch(domain:hastebin.com)",
      "WebFetch(domain:0x0.st)",
      "WebFetch(domain:transfer.sh)",
      "WebFetch(domain:file.io)",
      "WebFetch(domain:anonfiles.com)",
      "Edit(**/.env)",
      "Edit(**/.env.*)",
      "Edit(**/.ssh/**)",
      "Read(**/.env)",
      "Read(**/.env.*)",
      "Read(**/.ssh/**)",
      "Read(**/.aws/**)",
      "Read(**/.azure/**)",
      "Read(**/.kube/**)",
      "Read(**/.config/gcloud/**)",
      "Read(**/.docker/config.json)",
      "Read(**/.npmrc)",
      "Read(**/.pypirc)",
      "Read(**/.ai-safety/**)",
      "Edit(**/.ai-safety/**)",
      "Read(**/.deepseek-claude/**)",
      "Edit(**/.deepseek-claude/**)"
    ]
  },
  "_comment_sandbox": "Windows には sandbox 節を意図的に置いていない（mac 側 settings.mac.json には置いてある）。理由: Claude Code の純正サンドボックスは公式に macOS / Linux / WSL2 のみ対応で、ネイティブ Windows は非対応と明記されている（公式 https://code.claude.com/docs/en/sandboxing 「Native Windows is not supported. On Windows, run Claude Code inside a WSL2 distribution.」）。バイナリには Windows 実装（srt-win / WFP / NTFS ACL）が同梱されているが feature gate で無効化されており、実行時に『The Windows sandbox is not enabled on this build.』を返す。したがって Windows 側は v1.16.0 と同じく hook（guard-bash.ps1 / guard-write.ps1 / guard-webfetch.ps1 / guard-post-output.ps1 / guard-prompt.ps1 / guard-observe.ps1）＋ permissions リストで守る。mac との非対称はここだけで、保護の中身（deny 床・保護パス・秘密検出）は 3 エンジン共通の policy/safety-policy.json で同一。将来 Windows で feature gate が開いたら、この節を settings.mac.json と同じ内容に差し替える。",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-prompt.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if ($env:CLAUDE_PROJECT_DIR) { $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-observe.ps1'; if (Test-Path -LiteralPath $p) { & $p } } } catch { }; exit 0"
            ]
          }
        ]
      },
      {
        "matcher": "Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-bash.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-write.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      },
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-write.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      },
      {
        "matcher": "MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-write.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      },
      {
        "matcher": "NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-write.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      },
      {
        "matcher": "WebFetch",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-webfetch.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      }
    ],
    "PermissionRequest": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if ($env:CLAUDE_PROJECT_DIR) { $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-observe.ps1'; if (Test-Path -LiteralPath $p) { & $p } } } catch { }; exit 0"
            ]
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash|Read|WebFetch",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-post-output.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-post-output.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      }
    ]
  }
}
