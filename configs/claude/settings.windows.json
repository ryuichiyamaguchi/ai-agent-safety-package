{
  "env": {
    "AI_SAFE_ENGINE": "claude",
    "CLAUDE_CODE_NO_FLICKER": "1"
  },
  "cleanupPeriodDays": 120,
  "permissions": {
    "defaultMode": "dontAsk",
    "disableBypassPermissionsMode": "disable",
    "allow": [
      "Read(./**)",
      "LS(./**)",
      "Glob(./**)",
      "Grep(./**)",
      "Edit(./**)",
      "Write(./**)",
      "MultiEdit(./**)",
      "Bash(git status*)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(npm test*)",
      "Bash(npm run build*)",
      "Bash(npm run lint*)",
      "Bash(python -m pytest*)",
      "Bash(pytest*)",
      "WebFetch(domain:developers.openai.com)",
      "WebFetch(domain:docs.anthropic.com)",
      "WebFetch(domain:code.claude.com)",
      "WebFetch(domain:github.com)",
      "WebFetch(domain:raw.githubusercontent.com)",
      "WebFetch(domain:gemini.google.com)",
      "WebFetch(domain:ai.google.dev)",
      "WebFetch(domain:cloud.google.com)"
    ],
    "ask": [
      "Bash(*)"
    ],
    "deny": [
      "Bash(curl *)",
      "Bash(wget *)",
      "Bash(Invoke-WebRequest *)",
      "Bash(Invoke-RestMethod *)",
      "Bash(rm -rf *)",
      "Bash(Remove-Item * -Recurse * -Force *)",
      "Bash(git push *)",
      "Bash(npm publish *)",
      "WebFetch(domain:gist.github.com)",
      "WebFetch(domain:gist.githubusercontent.com)",
      "WebFetch(domain:pastebin.com)",
      "WebFetch(domain:hastebin.com)",
      "WebFetch(domain:0x0.st)",
      "WebFetch(domain:transfer.sh)",
      "WebFetch(domain:file.io)",
      "WebFetch(domain:anonfiles.com)",
      "Write(**/.env)",
      "Write(**/.env.*)",
      "Write(**/.ssh/**)",
      "Edit(**/.env)",
      "Edit(**/.env.*)",
      "Edit(**/.ssh/**)",
      "Read(**/.env)",
      "Read(**/.env.*)",
      "Read(**/.ssh/**)",
      "Read(**/.aws/**)",
      "Read(**/.azure/**)",
      "Read(**/.kube/**)",
      "Read(**/.config/gcloud/**)",
      "Read(**/.docker/config.json)",
      "Read(**/.npmrc)",
      "Read(**/.pypirc)",
      "Bash(Get-ChildItem Env:*)",
      "Bash(gci Env:*)",
      "Bash(dir Env:*)",
      "Bash(ls Env:*)",
      "Bash(printenv*)",
      "Bash(printenv *)",
      "Bash(env)",
      "Bash(set *)"
    ]
  },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-prompt.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-bash.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-write.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      },
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-write.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      },
      {
        "matcher": "MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-write.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      },
      {
        "matcher": "WebFetch",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-webfetch.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash|Read|WebFetch",
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-post-output.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "powershell.exe",
            "args": [
              "-NoProfile",
              "-ExecutionPolicy",
              "Bypass",
              "-Command",
              "try { if (!$env:CLAUDE_PROJECT_DIR) { throw 'CLAUDE_PROJECT_DIR missing' }; $p = Join-Path $env:CLAUDE_PROJECT_DIR '.ai-safety\\hooks\\windows\\guard-post-output.ps1'; if (!(Test-Path -LiteralPath $p)) { throw ('AI Safety hook missing: ' + $p) }; & $p; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } catch { [Console]::Error.WriteLine($_.Exception.Message); exit 2 }"
            ]
          }
        ]
      }
    ]
  }
}
